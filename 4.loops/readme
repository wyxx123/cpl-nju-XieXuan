出现的错误：
1.函数传值or循环时用错了，本来要用变量，用了常量
2.关于输入：可能给的数据有点bug，不用按回车，但是没关系，不影响结果（同样的，结尾是否换行也没影响）
但是  ”%c“可能匹配空格，最好用” %c“；（用getchar 匹配空格）
3.转移表：（arr中存放bias偏差）打表打错了（有个-1 1 写成了-1 -1）
4.关于输出：如果结果对了，oj上standard output也对了，那么bug在哪里呢？
要么是自己看错，结果其实是错的，那么慢慢对吧；如果真对了，那就是多输出了无法看见的字符（大多数是ASCII=0的）